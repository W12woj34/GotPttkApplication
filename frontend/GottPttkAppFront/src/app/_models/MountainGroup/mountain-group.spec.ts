import { MountainGroup } from './mountain-group';

describe('MountainGroup', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new MountainGroup()).toBeTruthy();
  });
});
