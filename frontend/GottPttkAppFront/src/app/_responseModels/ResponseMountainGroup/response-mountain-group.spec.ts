import { ResponseMountainGroup } from './response-mountain-group';

describe('ResponseMountainGroup', () => {
  it('should create an instance', () => {
    expect(new ResponseMountainGroup()).toBeTruthy();
  });
});
