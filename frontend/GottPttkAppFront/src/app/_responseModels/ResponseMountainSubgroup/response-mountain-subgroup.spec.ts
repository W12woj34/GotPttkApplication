import { ResponseMountainSubgroup } from './response-mountain-subgroup';

describe('ResponseMountainSubgroup', () => {
  it('should create an instance', () => {
    expect(new ResponseMountainSubgroup()).toBeTruthy();
  });
});
