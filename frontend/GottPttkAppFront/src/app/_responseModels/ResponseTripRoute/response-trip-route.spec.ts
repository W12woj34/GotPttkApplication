import { ResponseTripRoute } from './response-trip-route';

describe('ResponseTripRoute', () => {
  it('should create an instance', () => {
    expect(new ResponseTripRoute()).toBeTruthy();
  });
});
