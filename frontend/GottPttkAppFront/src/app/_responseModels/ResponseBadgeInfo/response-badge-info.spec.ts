import { ResponseBadgeInfo } from './response-badge-info';

describe('ResponseBadgeInfo', () => {
  it('should create an instance', () => {
    expect(new ResponseBadgeInfo()).toBeTruthy();
  });
});
